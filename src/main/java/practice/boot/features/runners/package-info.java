/**
 * All the runners will be invoked once the SpringApplication has
 * started. These runners will be called just before
 * <code>SpringApplication.run()</code> completes. Also we can set their order
 * using @Order annotation.
 * 
 */
package practice.boot.features.runners;