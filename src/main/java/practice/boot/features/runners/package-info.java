/**
 * All the runner classes will be invoked after the application started.
 * Application runners will behave as like code is written after
 * <code>SpringApplication.run()</code> inside main method. Also we can set
 * their order using @Order annotation.
 *
 */
package practice.boot.features.runners;